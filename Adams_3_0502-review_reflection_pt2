What is an array?
	a list of related items of the same datatypes
    How does one make a simple array of Strings?
    	String[][] Names = {{"Norvell", "Ray"}, {"Adams", "Hebert"}}
	System.out.println(names[0][0] + names[1][0]);	
    What is the syntax for a 2-D array?
	int[][] 2dArray;
	2dArray = new int [10][10];
    What is the syntax for a 3-D array?
	int[][][] 3dArray;
        3dArray = new int[10][10[10];
    What is the syntax for a while loop?
	while (true){
    
	}
    How does one change the value for the third value in a one dimensional array?[Give specific code]
	enemies [2] = [10]
    Why use while loops in your game?
	We would use a while loop in our game because it would keep the game going. This means
	we can keep printing the map and reprinting to the platyer can keep track of what they are
	doing.
    Your game should be in a method outside of main --- How will you call your game in main?
	I will call the drawing of the map the directions to go will most likely have its own
	method after revisitions of the code.
    Why was it recommended that you use .toLowerCase() with your Scanner input?
	Becuse then the player can imput anything and it still takes it as lower case avoiding invaild
	answeres
    You should use different methods when moving the protagonist and to check for 
intersections. How? Why?
	the intersection method like the slick2d game
    What values will you pass across methods? how? why?
	your coordinates, to make sure if the antaganist intersects.
